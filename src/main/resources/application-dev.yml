server:
  port: 8087

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/newpro
    username: postgres
    password: Karthik@0807
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  h2:
    console:
      enabled: true
      path: /h2-console

app:
  jwt:
    secret: KarthikSuperSecretKeyForJwtToken2025@NotesApp
    expirationSeconds: 3600
