server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://dpg-abcd1234-p12345.render.com:5432/notes_db
    username: notes_user
    password: notes_pass_123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

app:
  jwt:
    secret: sample-jwt-secret-1234567890
